export enum RequestMethod {
  GET = 'GET',
}

export enum InputType {
  TEXT = 'text',
  EMAIL = 'email',
  DATE = 'date',
  RADIO = 'radio',
  FILE = 'file',
  CHECKBOX = 'checkbox',
}

export enum InputName {
  NAME = 'name',
  TEXT = 'text',
  EMAIL = 'email',
  DATE = 'date',
  RADIO = 'radio',
  FILE = 'file',
  CHECKBOX = 'checkbox',
  SELECT = 'type',
}

export enum ButtonType {
  SUBMIT = 'submit',
  RESET = 'reset',
}
