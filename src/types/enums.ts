export enum RequestMethod {
  GET = 'GET',
}

export enum InputType {
  TEXT = 'text',
  EMAIL = 'email',
  DATE = 'date',
  RADIO = 'radio',
  FILE = 'file',
  CHECKBOX = 'checkbox',
}

export enum ButtonType {
  SUBMIT = 'submit',
  RESET = 'reset',
}
